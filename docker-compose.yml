version: "3"
services:
  todo-app:
    container_name: todo-app
    image: node/todo
    restart: always
    build: .
    ports:
      - "8080:8080"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:data/db
    ports:
      - '27018:27017'
